# explainers

Interactive HTML explainers for technical topics — papers, datasets, and concepts you can read, run, and grasp intuitively.

Published via GitHub Pages: https://littlemex.github.io/explainers/

## Series

| Series | Topic | Entry |
|--------|-------|-------|
| Skills | A catalog of my own Claude Code skills (slash commands) — what each does and when to fire it — so I don't have to remember them. Sub-series: Kaggle competition workflow; building corporate-template pptx decks out of editable native shapes | [skills/index.html](./skills/index.html) |
| English | Rebuilding English grammar around one picture per word instead of a list of translations. Two sub-series. Prepositions: 93 figures over seven pages, from `at`/`in`/`on` through movement, position, time, relation and cause, closing with a searchable map of all 146 English prepositions (98 single-word, 48 group). Basic verbs: 63 schemas over seven pages — `get` is a switch flipping from 0 to 1, `have` is being inside your reach — ending with phrasal verbs factored into verb figure times preposition figure | [english/index.html](./english/index.html) |
| Videos | Short captioned videos that show how a mechanism from a post or paper moves, one page per video with the source links and the embedded video; all video files live in `videos/media/` (episode 1: DFlash 2, parallel drafting with a pairwise path selector and a two-tap convolution) | [videos/index.html](./videos/index.html) |
| vLLM blog | Reading the official vLLM blog one post at a time in Japanese, citing the original figures and adding hands-on demos (episode 1: tiered KV cache offloading; episode 2: GLM 5.3 Hybrid HiSparse; episode 3: agentic serving measured on AgentX) | [vllm-blog-index.html](./vllm-blog-index.html) |
| Paper reading | Reading one notable paper/blog post at a time — background, core equations, and conclusions in a single beginner-friendly, interactive page (episode 1: Lilian Weng's scaling laws; episode 2: Kimi K3's architecture) | [paper-index.html](./paper-index.html) |
| ML basics | Transformer building blocks (Attention, FFN/MoE, normalization) and Flow Matching, each explained from scratch with live animations | [ml-basics-index.html](./ml-basics-index.html) |
| Attention lineage | MHA to GQA/MLA, sparsification (SWA/DSA), and linear/state-space models (Mamba), traced along a release-time family tree | [attention-series-index.html](./attention-series-index.html) |
| NKI Workshop | Writing high-performance kernels on AWS Trainium — hardware hierarchy, roofline, FlashAttention, tiling, low precision, collectives | [nki-workshop-index.html](./nki-workshop-index.html) |
| Coding | Algorithms and coding fundamentals — binary search, complexity, DP, graph search — explained then quizzed | [coding-index.html](./coding-index.html) |
| Distributed systems | CAP, consistency models, consensus (Raft/Paxos), replication — explained then quizzed | [distsys-index.html](./distsys-index.html) |
| MLIR | Dialects, progressive lowering, SSA, pattern rewriting — explained then quizzed | [mlir-index.html](./mlir-index.html) |
| Mathematical statistics | MLE, CLT, Bayesian inference, hypothesis testing — explained then quizzed | [mathstats-index.html](./mathstats-index.html) |
| Experiment series (hub) | Research-process records grouped as sub-series: cheap-model routing, pushing a small VLM with verifiers and RL, whether a decision model that generates no text is worth paying for, and the current state of gateway development | [experiments-index.html](./experiments-index.html) |
| Is a decision model worth paying for | Auditing the claims behind Jev, a model that generates no text, and then building the same shape and measuring it — why agreement with two frontier models cannot establish accuracy, why the backbone is probably a causal model with generation removed rather than an encoder, why calibration is not the same objective as accuracy (the vendor publishes 67.8% against 74.1%), and a measured prize of +0.2664 that no honest predictor reaches | [jev-index.html](./jev-index.html) |
| Cheap VLM, made strong | How far a small 7B VLM can be pushed with RL and verifiers — the verifier toolkit (episodes 1-15, including BNF-constrained decoding), measured GRPO results on OmniDocBench-JASyn (episode 16), what an inference-time selector recovers without labels (episode 17), and the correction that most of the apparent headroom was a hindsight-oracle artefact (episode 18) | [vlm-verifier-index.html](./vlm-verifier-index.html) |
| Cheap models, measured | Whether using the expensive model only where it matters actually saves money — why the same idea yields 24% and 58%, what a nested success set forbids, pricing a fixed-cost GPU box (and getting it wrong twice), and halving the input price by changing the tensor-parallel width | [gateway-index.html](./gateway-index.html) |
| Information theory | Entropy, KL divergence, mutual information, coding theorems — explained then quizzed | [infotheory-index.html](./infotheory-index.html) |
| All-to-all without ordering | Porting DeepEP's MoE dispatch/combine to a fabric that does not preserve order (AWS EFA / SRD) — who writes the WQE across ibverbs, libfabric, NVSHMEM, NCCL GIN and EFA-GDA; where the upstream tail-pointer scheme breaks; the in-band header plus counting-signal protocol and the three invariants its correctness rests on; how a 512-signal budget produces a 176-rank ceiling; whether the whole thing can be verified without an EFA instance; and a row-level oracle that independently recomputes routing per source rank and catches corrupted, dropped and reordered deliveries — retracting an earlier claim that the kernel tolerates a signal-before-data violation once combine is checked against exact sums; and moving the emulator's delivery to a CUDA context of its own, which removes the GDRCopy requirement entirely (at a measured cost, since a second context time-slices against a resident kernel) while a real EFA-GDA comparison still stalls on a vendor-side fabric-selection mismatch unrelated to DeepEP; and, since waiting after every copy from that context is what made the GDRCopy-less path 30x slower, an asynchronous delivery path (ordered by one shared stream, a lock-held signal total, and a per-connection send queue) that turns a 45-minute non-finish into 570 seconds, closing a use-after-free found along the way and retracting a first ring design a review found three holes in; and, having traced the intermittent teardown heap corruption that combination exposed through a corrupted chunk's stale header, AddressSanitizer, and a window-registration log, finding the actual cause in DeepEP itself (its NCCL context has no destructor, so a buffer discarded without `destroy()` leaves its window registered over memory glibc goes on to reuse) and confirming it by making the failure path call `destroy()` too; and, ruling out the emulator, DeepEP's host side, and its GPU kernels under AddressSanitizer and compute-sanitizer in turn, finding vLLM's own long-standing startup segfault and wrong answer under DeepEP V2 to be neither DeepEP's nor the emulator's but vLLM 0.28's `ncclCommProperties` declared 88 bytes short of what NCCL 2.31+ writes into it (already fixed upstream in vLLM 0.30.0, confirmed correct on two H100 hosts), confirmed by making vLLM serve the identical correct completion once that one declaration is fixed, and correcting two earlier episodes that had attributed the crash to DeepEP; and, extending from one host with two ranks to two real hosts with one rank each, vLLM serving that identical completion over DeepEP V2 and the emulator across an actual two-node boundary, after fixing an emulator bug where its own unlocked `getenv` on the progress path raced DeepEP's `setenv`, plus a negative control across those same two hosts (a forced ordering violation corrupts vLLM's answer and is caught by DeepEP's own GPU-side assertion), a retraction of an earlier claim about a real EFA fabric offering `FI_RX_CQ_DATA` (an experimental build still selected the other fabric, and probing capabilities one at a time found `efa-direct` drops `FI_HMEM`, which both the transport and GIN require, once added — so this hardware cannot reach it by any fix), and a retraction of an earlier claim that a September 2026 malformed vLLM completion was explained by the same heap overflow (corrupted communication produces the same kind of malformed text, so the two causes are indistinguishable from the output alone), plus an AddressSanitizer build of the emulator passing DeepEP's own two-host test with and without GDRCopy at zero reports, and a note that the same attempt under vLLM never reached DeepEP at all (cuDNN fails to load under the preload, unrelated to the emulator), plus a written-but-never-run concurrent-request check (32 identical requests must all match the single-request answer) on the two-host path whose run was cut short by a Capacity Block that terminated both hosts about 30 minutes before its own end time, and the same check run to completion on a single GPU with two ranks instead (vLLM 0.30.0, no local patch: 128 concurrent requests all matched in 36 s, 8 consecutive runs with and without GDRCopy all matched with no recurrence of a startup crash seen once under 0.28); and a 32-item process log of every pitfall this verification hit, in the task runner, the instruments, and the environment, each as what happened, why it looked like something else, and how to avoid it — with no environment-specific values | [deepep-efa-index.html](./deepep-efa-index.html) |
| Prismyra measurements | A work-record for Prismyra, a library that reads a document once and answers many closed questions from the logits in a single forward pass — removing a "paged" cache path that never actually ran (a suspiciously perfect 0.0000 probability match gave it away), finding that a "width doesn't change latency" claim only held at short context (1.51x at 24k tokens), fixing two memory-admission blind spots, and the finding that the core claim "read-out answers are indistinguishable from generated ones" breaks down at long context (-5.0%, CI excluding zero at 10k tokens) — then losing a head-to-head against vLLM by 4.3x, tracing it to 32,873 kernel launches rather than to any kernel, and closing it to 1.9x plus a recorded-and-replayed branch pass at 32.6 ms against 113.5, with a side-by-side of ikermoel/open-alternative-jev whose own measurements say plain vLLM beats their default mode | [prismyra-measurements-explainer.html](./prismyra-measurements-explainer.html) |

### ML basics series

1. [Softmax Attention](./softmax-attention-explainer.html) — how Q/K/V mix, why the cost is N², bound analysis, and why FlashAttention works
2. [FFN to MoE](./ffn-moe-explainer.html) — the FFN workhorse vs. sparsely-activated MoE, and the GEMM-inefficiency → block-sparse (MegaBlocks) story
3. [RMSNorm](./rmsnorm-explainer.html) — why dropping the mean keeps quality while simplifying the implementation; cost and fused-kernel effects vs. LayerNorm
4. [Flow Matching](./flow-matching-explainer.html) — watch noise flow into data, and see how averaging straight-line paths yields the correct velocity field
5. [Positional encoding: RoPE and NoPE](./rope-explainer.html) — why attention ignores order, how RoPE rotates Q/K so the dot product depends only on relative distance, the frequency hierarchy, context extension (PI/NTK/YaRN), and the NoPE "maybe you don't need it" result

## How it works

Each explainer is a single self-contained HTML file (inline CSS/JS, no build step). Open any file directly in a browser, or browse them from `index.html`.

The one exception is the English series: its pages share `english/lib/fig.css` and `english/lib/fig-engine.js` (the figure primitives, one drawing convention across every sub-series), plus one data file per sub-series (`english/prepositions/prep-data.js`, `english/basic-verbs/verb-data.js`) holding every card tagged by page. Copying one dataset into seven files would guarantee they drift apart. Still no build step — the pages load those files relatively.

## Authoring rule: quiz-based explainer series

The Coding, Distributed systems, MLIR, Mathematical statistics, and Information theory series follow a fixed format. Every page in these series MUST:

1. **Explain first, graphically.** Open with an explanation section that builds intuition for *why* things are true, aimed at a beginner. Use inline SVG figures (not just prose) and typeset math with LaTeX via MathJax (`$...$` inline, `$$...$$` display). Prefer showing the mechanism visually over stating conclusions.
2. **Then quiz: 10 questions, 5 options each.** After the explanation, present exactly 10 multiple-choice questions, each with 5 options.
3. **Click an option → reveal detailed explanations for all five.** When the reader picks an option, every option expands with a detailed explanation: the correct one explains *why it is correct* (from definitions / invariants); each distractor explains *why it is wrong*, naming the common misconception it targets. A figure-based summary ("why this answer") follows, again using SVG + LaTeX.
4. **Be self-contained.** Single HTML file, inline CSS/JS, MathJax from CDN only. No emoji. Japanese prose. In JS string literals, escape LaTeX backslashes (`'$\\log_2 n$'`).

The reference implementation lives in `_template/QUIZ_TEMPLATE.html` (in the working tree used to author pages) — copy its CSS and the quiz-rendering JS engine verbatim and only fill in the `QUIZ` array and explanation content. This keeps the interaction identical across every series.

## Publishing

GitHub Pages serves the repository root of the `main` branch. Add a new `*.html` file, link it from `index.html` (and the relevant series index), and push.
